# Archived Specs

These specs were useful while implementing earlier slices, but they are no longer the primary source
of truth for current architecture or acceptance criteria.

Use the active documents listed in `docs/INDEX.md` before relying on anything in this directory.
