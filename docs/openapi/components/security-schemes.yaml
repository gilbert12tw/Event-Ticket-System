ProviderBearerAuth:
  type: http
  scheme: bearer
  bearerFormat: HMAC provider token
  description: External provider token containing required employee claims and an HMAC-SHA256 signature verified with runtime provider secret config.
