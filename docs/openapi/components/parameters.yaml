EventId:
  name: event_id
  in: path
  required: true
  schema:
    type: string
    format: uuid
RegistrationId:
  name: registration_id
  in: path
  required: true
  schema:
    type: string
    format: uuid
TicketId:
  name: ticket_id
  in: path
  required: true
  schema:
    type: string
    format: uuid
ReviewId:
  name: review_id
  in: path
  required: true
  schema:
    type: string
    format: uuid
DeliveryId:
  name: delivery_id
  in: path
  required: true
  schema:
    type: string
    pattern: "^del_[a-f0-9]{32}$"
ExportId:
  name: export_id
  in: path
  required: true
  schema:
    type: string
    format: uuid
